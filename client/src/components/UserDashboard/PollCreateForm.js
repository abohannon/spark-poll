import React from 'react';
import { Card } from '../common';

const styles = {
  containerStyle: {

  },
};

const PollCreateForm = () => (
  <div className="poll-create-form" style={styles.containerStyle}>
    <Card type="wide">
      <h3>Poll Create Form</h3>
    </Card>
  </div>
);

export default PollCreateForm;
